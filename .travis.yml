language: node_js
node_js: "8"

os:
  - linux
  - osx

cache: yarn

install:
  - yarn

script:
  - yarn test
  - yarn coverage
  - yarn coverage-report
  - npm install codecov -g
  - codecov
  - yarn build