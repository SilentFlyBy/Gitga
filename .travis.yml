language: node_js
node_js: "8"

python:
  - "2.7"

os:
  - linux

cache: yarn

install:
  - yarn

before_script:
  - export DISPLAY=:99.0; sh -e /etc/init.d/xvfb start

script:
  - yarn test
  - yarn build