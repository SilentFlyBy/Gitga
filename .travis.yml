language: node_js
node_js: "8"

python:
  - "2.7"

os:
  - linux
  - osx

cache: yarn

install:
  - yarn

script:
  - yarn coverage
  - yarn coverage-report
  - npm install codecov -g
  - codecov
  - yarn build